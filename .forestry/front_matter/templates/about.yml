---
label: About
hide_body: true
fields:
- name: what
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html-blocks
  label: What We Do
- name: who
  type: blocks
  label: Who We Are
  template_types:
  - team-member
  config:
    min: 
    max: 
- name: who-we-are
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: Name
  - name: position
    type: text
    config:
      required: false
    label: Position
  config:
    min: 
    max: 
    labelField: 
  label: who-we-are
pages:
- about.html
